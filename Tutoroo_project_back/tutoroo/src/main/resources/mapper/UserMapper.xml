<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tutoroo.mapper.UserMapper">

    <insert id="save" parameterType="com.tutoroo.entity.UserEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            username, password, name, email, phone, parent_phone, age, gender,
            role, provider, provider_id, membership_tier, total_point, point_balance,
            level, exp, profile_image, current_streak, last_study_date,
            status, created_at, updated_at
        ) VALUES (
                     #{username}, #{password}, #{name}, #{email}, #{phone}, #{parentPhone}, #{age}, #{gender},
                     #{role}, #{provider}, #{providerId}, #{membershipTier}, #{totalPoint}, #{pointBalance},
                     #{level}, #{exp}, #{profileImage}, 0, NULL,
                     'ACTIVE', NOW(), NOW()
                 )
    </insert>

    <select id="countAllUsers" resultType="int">
        SELECT count(*) FROM users
    </select>

    <select id="findById" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByUsername" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <update id="update" parameterType="com.tutoroo.entity.UserEntity">
        UPDATE users
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="parentPhone != null">parent_phone = #{parentPhone},</if>
            <if test="profileImage != null">profile_image = #{profileImage},</if>
            <if test="age != null">age = #{age},</if>

            <if test="membershipTier != null">membership_tier = #{membershipTier},</if>
            <if test="totalPoint != null">total_point = #{totalPoint},</if>
            <if test="pointBalance != null">point_balance = #{pointBalance},</if>
            <if test="level != null">level = #{level},</if>
            <if test="exp != null">exp = #{exp},</if>

            <if test="currentStreak != null">current_streak = #{currentStreak},</if>
            <if test="lastStudyDate != null">last_study_date = #{lastStudyDate},</if>
            <if test="rivalId != null">rival_id = #{rivalId},</if>

            <if test="status != null">status = #{status},</if>
            <if test="withdrawalReason != null">withdrawal_reason = #{withdrawalReason},</if>
            <if test="deletedAt != null">deleted_at = #{deletedAt},</if>

            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="findByNameAndEmailAndPhone" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users WHERE name = #{name} AND email = #{email} AND phone = #{phone}
    </select>

    <select id="findByUsernameAndEmail" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users WHERE username = #{username} AND email = #{email}
    </select>

    <update id="updatePassword">
        UPDATE users SET password = #{password}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="updateUserContact">
        UPDATE users SET phone = #{phone}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="updateSocialUser">
        UPDATE users
        SET
            name = #{name},
            phone = #{phone},
            parent_phone = #{parentPhone},
            age = #{age},
            gender = #{gender},
            role = #{role},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="earnPoints">
        UPDATE users
        SET total_point = total_point + #{amount},
            point_balance = point_balance + #{amount},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

    <update id="spendPoints">
        UPDATE users
        SET point_balance = point_balance - #{amount},
            updated_at = NOW()
        WHERE id = #{userId}
    </update>

    <update id="updateUserPointByPlan">
        UPDATE users
        SET point_balance = point_balance + #{point},
            updated_at = NOW()
        WHERE id = (SELECT user_id FROM study_plans WHERE id = #{planId})
    </update>

    <update id="resetRankingPoints">
        UPDATE users SET daily_rank = NULL
    </update>

    <select id="getRankingList" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users
        <where>
            status = 'ACTIVE'
            <if test="gender != null">AND gender = #{gender}</if>
            <if test="ageGroup != null">
                <choose>
                    <when test="ageGroup == 10">AND age BETWEEN 10 AND 19</when>
                    <when test="ageGroup == 20">AND age BETWEEN 20 AND 29</when>
                    <when test="ageGroup == 30">AND age BETWEEN 30 AND 39</when>
                    <when test="ageGroup == 40">AND age BETWEEN 40 AND 49</when>
                    <when test="ageGroup == 50">AND age BETWEEN 50 AND 59</when>
                    <when test="ageGroup == 60">AND age >= 60</when>
                    <otherwise>AND age >= #{ageGroup} AND age &lt; #{ageGroup} + 10</otherwise>
                </choose>
            </if>
        </where>
        ORDER BY total_point DESC LIMIT 100
    </select>

    <select id="findAllByOrderByTotalPointDesc" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users WHERE status = 'ACTIVE' ORDER BY total_point DESC
    </select>

    <select id="findPotentialRival" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users
        WHERE id != #{myId}
          AND rival_id IS NULL
          AND status = 'ACTIVE'
          AND total_point BETWEEN #{myPoint} - 200 AND #{myPoint} + 200
        ORDER BY RAND() LIMIT 1
    </select>

    <select id="findUsersForWeeklyReport" resultType="com.tutoroo.entity.UserEntity">
        SELECT u.* FROM users u
        WHERE u.parent_phone IS NOT NULL
          AND u.status = 'ACTIVE'
          AND EXISTS (
            SELECT 1 FROM study_plans p
            WHERE p.user_id = u.id AND p.status = 'PROCEEDING'
        )
    </select>

    <select id="findWithdrawnUsersForPurge" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users
        WHERE status = 'WITHDRAWN'
          AND deleted_at &lt; DATE_SUB(NOW(), INTERVAL 90 DAY)
    </select>

    <delete id="deleteUserPermanently">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="findAllByIds" resultType="com.tutoroo.entity.UserEntity">
        SELECT * FROM users
        WHERE id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY FIELD(id,
        <foreach item="id" collection="ids" separator=",">
            #{id}
        </foreach>
        )
    </select>

</mapper>